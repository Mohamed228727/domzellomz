specVersion: "v1beta"
serviceId: "egypttest"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "egypttest-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
